---
- name: Generate Device Configuration
  connection: local
  hosts: device_roles_access_switch,device_roles_distribution_switch 
  gather_facts: False

  roles:
    - role: generate_config
      tags: generate_config 
